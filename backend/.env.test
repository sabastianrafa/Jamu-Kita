# Use SQLite in-memory for testing (works in CI/CD without MySQL)
DATABASE_URL="file::memory:?socket_timeout=10&connection_limit=1"

JWT_SECRET="test_jwt_secret_key_for_testing_only"
PORT=3001
NODE_ENV=test
